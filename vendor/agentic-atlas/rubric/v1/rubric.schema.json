{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/adamcaviness/agentic-atlas/rubric/v1/rubric.schema.json",
  "title": "Agentic Atlas Rubric Manifest",
  "type": "object",
  "required": ["rubric_version", "title", "axes"],
  "additionalProperties": false,
  "properties": {
    "rubric_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semver of the rubric. See docs/versioning.md."
    },
    "title": { "type": "string" },
    "description": { "type": "string" },
    "scale": {
      "type": "number",
      "exclusiveMinimum": 0,
      "default": 10,
      "description": "Half-range for every axis: positions run -scale..+scale. Shared by all axes so they stay comparable. A display multiplier on the normalized weighted mean, it does not change any relative position."
    },
    "axes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$",
        "description": "Axis id, matching a directory under axes/."
      },
      "uniqueItems": true
    }
  }
}
